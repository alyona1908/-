#include <iostream>
 
using namespace std;
 
int main()
{
  int *mass, N, kol = 0, M,p;
 
/*  cout << "VVedite (N >0) N = ";*/
  cin >> N;
 
  mass = new int[N];
 
  for(int i = 0; i < N; i++)
  {
    //cout << "mass[" << i << "]=";
    cin >> mass[i];
  }
 /*for(int i = 0; i < N; i++)
 {
 mass[i]=i;	
cout << "mass[" << i << "]=";
cout<< mass[i]<<endl;
	
 }*/
 
/*  cout << "VVedite iskomoe chislo M = ";*/
  cin >> M;
 
 for(int i = 0; i < N; i++)
  {
    if(mass[i] == M)
    {
     kol++;
    }
  }
   cout <<kol;
 
  delete mass;
    return 0;
}
